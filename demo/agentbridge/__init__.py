"""AgentBridge — the agent-transactability layer for online stores.

Wraps any store backend (via StoreAdapter) in a safe, structured MCP
surface that AI shopping agents can reliably transact against.
"""

__version__ = "0.1.0"
