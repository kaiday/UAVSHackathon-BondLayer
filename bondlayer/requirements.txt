# Mirrors [project.dependencies] in pyproject.toml. Prefer `pip install -e .[dev]`.
# Unpinned lower bounds on purpose: exact pins have no wheels for newer
# Pythons (3.14) and fail to install on a judge's laptop.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
cryptography>=42.0
httpx>=0.27
python-multipart>=0.0.9
pypdf>=4.0
openai>=2.0
python-dotenv>=1.0
# WS-A: similarity dependency goes here
# dev
pytest>=8.0
